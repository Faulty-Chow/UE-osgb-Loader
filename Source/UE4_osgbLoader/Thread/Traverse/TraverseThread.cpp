#include "TraverseThread"
#include "../../Database/Model"

void TraverseThread::run()
{
	/*do
	{
		if (_frameCompelete)
		{
			_frameTick.wait(&_frameMutex);

			_frameMutex.unlock();
			_frameCompelete
		}
		

		_frameCompelete.exchange(1);

	} while (!testCancel() && !_bDone);*/
}